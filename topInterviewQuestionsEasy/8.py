from typing import List

class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        zeroCount = 0
        listCopy = list(nums)
        for val in List:
            if val == 0:
                
